import {UserDomain} from "../model/Domains"


export default class UserServices{

    static register(data,client){
        console.log(data)
    }
    static unregister(client){
        // console.log(client)
    }
}